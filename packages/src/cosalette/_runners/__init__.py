"""Runner sub-package: telemetry, stream, trigger, and shared utilities.

Internal package containing runner implementations. Modules should import
directly from sub-modules rather than through this package init.
"""

from __future__ import annotations
