asyncapi: 3.0.0
info:
  title: test
  version: 0.1.0
components:
  schemas:
    A:
      $ref: '#/components/schemas/B'
    B:
      $ref: '#/components/schemas/A'
channels:
  test:
    address: test/topic
    messages:
      msg:
        payload:
          $ref: '#/components/schemas/A'
