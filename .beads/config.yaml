# Beads Configuration File
# This file configures default behavior for all bd commands in this repository
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags
#
# PROJECT NOTE (cosalette): issue data lives in the Dolt DB (.beads/dolt/) and is
# exchanged over the Dolt remote — `bd dolt push` / `bd dolt pull` (task beads:push
# / task beads:pull) against the GitHub origin. The ref lives on the REMOTE only:
# `git ls-remote origin 'refs/dolt/*'` shows it; a local `git for-each-ref refs/dolt`
# is empty by design. .beads/issues.jsonl is a LOCAL-ONLY export, gitignored and
# untracked (2026-08 security audit finding F-SC1 / CWE-359 — the register itself is
# private, see SECURITY.md); it is never the source of truth here and is never
# committed. The commented-out `no-db` and `sync-branch` options below are upstream
# defaults documented by bd init; neither is used by this project (and `bd sync` no
# longer exists as of bd 1.1.2).
#
# WARNING: the commented-out `no-*` keys below (`no-db`, `no-daemon`, `no-auto-flush`,
# `no-auto-import`) are stale bd-init boilerplate — none of them exists in bd 1.1.2,
# so uncommenting one is a silent no-op. Verify any key against `bd config show`,
# which reports both the value and which file it came from.

# Issue prefix for this repository (used by bd init)
# If not set, bd init will auto-detect from directory name
# Example: issue-prefix: "myproject" creates issues like "myproject-1", "myproject-2", etc.
# Note: bd config uses issue_prefix (underscore) internally; this YAML key is informational.
issue-prefix: 'cos'
# Use no-db mode: load from JSONL, no SQLite, write back after each command
# When true, bd will use .beads/issues.jsonl as the source of truth
# instead of SQLite database
# no-db: false

# Disable daemon for RPC communication (forces direct database access)
# no-daemon: false

# Disable auto-flush of database to JSONL after mutations
# no-auto-flush: false

# Disable auto-import from JSONL when it's newer than database
# NOTE: `no-auto-import` is NOT a real bd 1.1.2 key (see the WARNING above); the key
# that works is `import.auto`, set for real further down this file.
# no-auto-import: false

# Enable JSON output by default
# json: false

# Default actor for audit trails (overridden by BD_ACTOR or --actor)
# actor: ""

# Path to database (overridden by BEADS_DB or --db)
# db: ""

# Auto-start daemon if not running (can also use BEADS_AUTO_START_DAEMON)
# auto-start-daemon: true

# Debounce interval for auto-flush (can also use BEADS_FLUSH_DEBOUNCE)
# flush-debounce: "5s"

# Export events (audit trail) to .beads/events.jsonl on each flush/sync
# When enabled, new events are appended incrementally using a high-water mark.
# Use 'bd export --events' to trigger manually regardless of this setting.
# events-export: false

# Git branch for beads commits (bd sync will commit to this branch)
# IMPORTANT: Set this for team projects so all clones use the same sync branch.
# This setting persists across clones (unlike database config which is gitignored).
# Can also use BEADS_SYNC_BRANCH env var for local override.
# If not set, bd sync will require you to run 'bd config set sync.branch <branch>'.
# sync-branch: "beads-sync"

# Multi-repo configuration (experimental - bd-307)
# Allows hydrating from multiple repositories and routing writes to the correct JSONL
# repos:
#   primary: "."  # Primary repo (where this database lives)
#   additional:   # Additional repos to hydrate from (read-only)
#     - ~/beads-planning  # Personal planning repo
#     - ~/work-planning   # Work planning repo

# Integration settings (access with 'bd config get/set')
# These are stored in the database, not in this file:
# - jira.url
# - jira.project
# - linear.url
# - linear.api-key
# - github.org
# - github.repo

# ── Project-set overrides (verified against `bd config show`) ────────────────
# The JSONL export is never an input here: the Dolt DB is the source of truth.
# bd's default is import.auto=true, which imports .beads/issues.jsonl over the DB
# whenever that file looks newer. The checked-in export path points at a snapshot
# that is regenerated by `task beads:sync` and can easily be older or newer than the
# DB by accident, so a stray touch/restore of it could silently revert issue state.
# Verify with: bd config show | grep import.auto   ->   false   (config.yaml)
import.auto: false
